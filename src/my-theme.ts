import { DefaultTheme } from "styled-components";

const myTheme: DefaultTheme = {
  borderRadius: "10px",
  padding: "10px",
  colors: {
    main: "red",
    secondary: "blue",
  },
};

export { myTheme };
